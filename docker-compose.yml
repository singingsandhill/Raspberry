services:
  eureka-server:
    image: softbank/server
    container_name: eureka-server
    ports:
      - "8761:8761"
    networks:
      - microservices

  gateway:
    image: softbank/gateway
    container_name: gateway
    ports:
      - "8080:8080"
    depends_on:
      - eureka-server
    networks:
      - microservices
    environment:
      - eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/

  frontend:
    image: softbank/fe
    container_name: frontend
    ports:
      - "8081:8081"
    depends_on:
      - eureka-server
    networks:
      - microservices
    environment:
      - eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/

  deploy:
    image: softbank/deploy
    container_name: deploy
    ports:
      - "8082:8082"
    depends_on:
      - eureka-server
    networks:
      - microservices
    environment:
      - eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/

  user:
    image: softbank/user
    container_name: user
    ports:
      - "8083:8083"
    depends_on:
      - eureka-server
    networks:
      - microservices
    environment:
      - eureka.client.service-url.defaultZone=http://eureka-server:8761/eureka/

networks:
  microservices:
    driver: bridge

volumes:
  eureka-data: