name: CI/CD Pipeline for Microservices

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
    contents: read
    security-events: write

env:
  DOCKER_REGISTRY: softbank2025  # DockerHub username
  IMAGE_TAG: ${{ github.sha }}

jobs:
  # Job 1: 전체 프로젝트 빌드 및 테스트 (변경 없음)
  build-and-test:
    name: Build and Test All Services
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Build and Test All Services
        run: ./gradlew build -x test

      - name: Run Tests
        run: ./gradlew test
        
      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: |
            server/build/libs/*.jar
            gateway/build/libs/*.jar
            fe/build/libs/*.jar
            deploy/build/libs/*.jar
            user/build/libs/*.jar
          retention-days: 1

  # Job 2: 서비스별 Docker 이미지 빌드 및 스캔 (변경 없음)
  docker-build-scan-push:
    name: Docker Build, Scan & Push
    needs: build-and-test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [server, gateway, fe, deploy, user]
      fail-fast: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image for ${{ matrix.service }}
        run: |
          IMAGE_NAME="${{ env.DOCKER_REGISTRY }}/${{ matrix.service }}"
          docker build \
            -t $IMAGE_NAME:${{ env.IMAGE_TAG }} \
            -t $IMAGE_NAME:latest \
            -f ./${{ matrix.service }}/Dockerfile
            .
          echo "✅ Image built: $IMAGE_NAME:${{ env.IMAGE_TAG }}"

      - name: Run Trivy Security Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.DOCKER_REGISTRY }}/${{ matrix.service }}:${{ env.IMAGE_TAG }}
          format: 'sarif'
          output: 'trivy-results-${{ matrix.service }}.sarif'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'

      - name: Upload Trivy Results to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        continue-on-error: true
        with:
          sarif_file: 'trivy-results-${{ matrix.service }}.sarif'
          category: 'trivy-${{ matrix.service }}'

      - name: Run Trivy Scan (Table Format)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.DOCKER_REGISTRY }}/${{ matrix.service }}:${{ env.IMAGE_TAG }}
          format: 'table'
          severity: 'CRITICAL,HIGH'

      - name: Push Docker Images
        run: |
          IMAGE_NAME="${{ env.DOCKER_REGISTRY }}/${{ matrix.service }}"
          docker push $IMAGE_NAME:${{ env.IMAGE_TAG }}
          docker push $IMAGE_NAME:latest
          echo "✅ Pushed: $IMAGE_NAME:${{ env.IMAGE_TAG }} and latest"

  # Job 3: EC2 배포 (AWS 자격증명 및 SSM 방식으로 수정됨)
  deploy-to-ec2:
    name: Deploy to EC2
    needs: docker-build-scan-push
    runs-on: ubuntu-latest
    if: (github.ref == 'refs/heads/main') && github.event_name == 'push'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # docker-compose.yml 업데이트 (이미지 태그 변경)
      - name: Update docker-compose.yml with new image tags
        run: |
          sed -i "s|image: softbank2025/server.*|image: softbank2025/server:${{ env.IMAGE_TAG }}|g" docker-compose.yml
          sed -i "s|image: softbank2025/gateway.*|image: softbank2025/gateway:${{ env.IMAGE_TAG }}|g" docker-compose.yml
          sed -i "s|image: softbank2025/fe.*|image: softbank2025/fe:${{ env.IMAGE_TAG }}|g" docker-compose.yml
          sed -i "s|image: softbank2025/deploy.*|image: softbank2025/deploy:${{ env.IMAGE_TAG }}|g" docker-compose.yml
          sed -i "s|image: softbank2025/user.*|image: softbank2025/user:${{ env.IMAGE_TAG }}|g" docker-compose.yml
          echo "📝 Updated docker-compose.yml:"
          cat docker-compose.yml

      # [!!] SSH 방식 대신 AWS 자격증명 설정 [!!]
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }} # 예: ap-northeast-2

      # [!!] SSH 방식 대신 AWS SSM Run Command로 배포 [!!]
      - name: Deploy to EC2 via AWS SSM
        uses: aws-actions/aws-ssm-send-command@v1
        with:
          instance-ids: ${{ secrets.EC2_INSTANCE_ID }} # GitHub Secret에 EC2 인스턴스 ID(i-...)를 등록해야 합니다.
          command: |
            #!/bin/bash
            set -e  # 에러 발생 시 즉시 중단

            # EC2 인스턴스 내의 실제 프로젝트 경로로 수정하세요.
            echo "📂 Navigating to project directory..."
            cd /home/ubuntu/Raspberry

            echo "🔧 Changing file ownership..."
            sudo chown -R $USER:$USER .

            echo "🔄 Pulling latest code..."
            git pull origin ${{ github.ref_name }}
            
            # === 💡 스왑 메모리 설정 시작 ===
            if [ -f /swapfile ]; then
              echo "ℹ️ Swap file /swapfile already exists. Skipping creation."
            else
              echo "🚀 Creating 2G swap file..."
              sudo fallocate -l 2G /swapfile
              sudo chmod 600 /swapfile
              sudo mkswap /swapfile
            fi
            if ! sudo swapon --show | grep -q '/swapfile'; then
              echo "🟢 Activating swap..."
              sudo swapon /swapfile
            fi
            if ! grep -q '/swapfile' /etc/fstab; then
              echo "✏️ Adding swap to /etc/fstab for persistence..."
              echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
            fi
            echo "📊 Current swap status:"
            sudo swapon --show
            free -h
            # === 스왑 메모리 설정 끝 ===

            echo "🐳 Logging in to Docker Hub..."
            echo "${{ secrets.DOCKERHUB_PASSWORD }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

            echo "🔧 Granting Docker socket permissions..."
            sudo chmod 666 /var/run/docker.sock

            echo "📥 Pulling latest Docker images..."
            docker-compose pull

            echo "🛑 Stopping and removing corrupted containers..."
            docker-compose down

            echo "🚀 Starting services with new images..."
            docker-compose up -d --remove-orphans

            echo "🧹 Cleaning up old images..."
            docker image prune -a -f

            echo "⏳ Waiting for services to start..."
            sleep 15

            echo "✅ Deployment completed!"
            docker-compose ps

      # [!!] SSH 방식 대신 AWS SSM으로 배포 검증 [!!]
      - name: Verify Deployment
        uses: aws-actions/aws-ssm-send-command@v1
        with:
          instance-ids: ${{ secrets.EC2_INSTANCE_ID }}
          command: |
            #!/bin/bash
            set -e

            echo "⏳ Waiting for services to initialize..."
            sleep 50

            cd /home/ubuntu/Raspberry

            echo "🔍 Checking service health..."

            # Eureka Server 체크
            if curl -sf http://localhost:8761 > /dev/null 2>&1; then
              echo "✅ Eureka Server is healthy"
            else
              echo "❌ Eureka Server health check failed"
              exit 1
            fi

            # Gateway 체크
            if curl -sf http://localhost:8080/actuator/health > /dev/null 2>&1; then
              echo "✅ Gateway is healthy"
            else
              echo "⚠️ Gateway health check failed"
            fi

            echo "📊 Service Status:"
            docker-compose ps

      # Slack 알림 (변경 없음)
      - name: Notify Deployment Status
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ Deployment successful to EC2"
          else
            echo "❌ Deployment failed!"
            exit 1
          fi
